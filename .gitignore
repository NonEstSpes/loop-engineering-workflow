.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.env
.DS_Store
*.log

# Local-only CA certificates (corporate / self-signed) — never commit
*.pem
*.crt

# ZCode local workspace state
.zcode/

# TODO.md is generated/personal task backlog (tracker tasks + human entries).
# The format is documented in README.md; each user keeps their own copy.
# Anchored to repo root so docs/examples TODO files are not accidentally ignored.
/TODO.md

# Daemon runtime artifacts
.devflow/
logs/
